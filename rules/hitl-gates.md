# molecule-careful-bash — HITL Gate Rules

This plugin does not implement HITL gates. This file is a placeholder for future
rule extensions if the plugin adds approval flows for certain bash operations.

## Current Scope

`molecule-careful-bash` uses the `pre-bash-careful` hook only — no approval flow.
See `rules/careful-bash.md` for the active rule set.
